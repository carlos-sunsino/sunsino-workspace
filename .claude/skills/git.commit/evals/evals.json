{
  "skill_name": "git.commit",
  "evals": [
    {
      "id": 1,
      "prompt": "我在 `src/api/users.ts` 裡修掉了一個 null pointer bug——當 userId 不存在時會直接 crash，我加了 optional chaining 處理。幫我 commit 這個改動。",
      "expected_output": "skill 應檢視工作樹、暫存 src/api/users.ts、生成符合 Conventional Commits 格式的 fix 型提交訊息（繁體中文 description、body 條列說明修正內容與原因），並向使用者確認後執行提交",
      "files": []
    },
    {
      "id": 2,
      "prompt": "我最近改了好幾個東西，git status 看起來有點亂。有新功能（新增了 `src/features/export.ts`），也把 `package.json` 裡的 `axios` 從 1.4 升到 1.7，還順手調整了一些 `README.md` 的格式。幫我整理一下 commit。",
      "expected_output": "skill 應識別三種不相關的變更並提出拆分計畫（feat / chore / docs 各一個 commit），向使用者確認後依序暫存、生成訊息並逐一提交",
      "files": []
    },
    {
      "id": 3,
      "prompt": "`src/utils/date.ts` 裡我做了兩件事：修正了 `formatDate()` 的時區計算有誤，還順便把整個檔案的縮排從 2 spaces 改成 4 spaces。這兩件事我想分開 commit，你來處理吧。",
      "expected_output": "skill 應說明需要用 git add -p 進行 hunk 層級暫存，先暫存邏輯變更生成 fix commit，再暫存格式變更生成 style commit，兩個 commit 各自有正確的 type 與 body",
      "files": []
    }
  ]
}
